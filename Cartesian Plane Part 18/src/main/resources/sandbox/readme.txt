The most import us-state... file in this directory is us-state-all-data.csv.
This is the input file for creating a list of States. I was compiled piecemeal
from the other files, which are probably no longer needed.